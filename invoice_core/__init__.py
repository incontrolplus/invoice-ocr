"""Bulgarian Invoice OCR & Document Understanding Pipeline (Core Package)."""
from __future__ import annotations

from . import constants, currency, tesseract_env, models, normalizers, ingestion, preprocessing, ocr_passes, layout, classification, extraction, table_recovery, financials, validation, cache, pipeline, batch, cli, vendor_profiles

from .constants import (
    ALL_COLUMN_SYNONYMS_SORTED,
    BULGARIAN_KEYWORDS,
    COLUMN_SYNONYMS,
    CONTINUATION_KEYWORDS,
    CONTINUATION_PATTERNS,
    DEFAULT_OCR_CACHE_DIR,
    DEFAULT_OCR_LANG,
    DEFAULT_RASTER_DPI,
    DUAL_DISPLAY_END_DATE,
    Decimal,
    EUR_MANDATORY_DATE,
    FIXED_EUR_BGN_RATE,
    IMAGE_EXTENSIONS,
    KNOWN_TESSDATA_LOCATIONS,
    MIN_CONFIDENCE,
    PDF_EXTENSIONS,
    PROJECT_ROOT,
    Path,
    RECEIPT_KEYWORDS,
    RECIPIENT_KEYWORDS,
    RECIPIENT_KEYWORD_RE,
    SUMMARY_KEYWORDS,
    SUPPLIER_KEYWORDS,
    SUPPLIER_KEYWORD_RE,
    SUPPORTED_EXTENSIONS,
    TOTAL_TOLERANCE,
    VAT_TOLERANCE,
    ZDDS_DISCOUNT_TOLERANCE,
    annotations,
    logger,
    logging,
    os,
    re,
)

from .currency import (
    Decimal,
    FIXED_EUR_BGN_RATE,
    ROUND_HALF_UP,
    annotations,
    convert_bgn_to_eur,
    convert_eur_to_bgn,
    verify_dual_currency_parity,
)

from .tesseract_env import (
    DEFAULT_OCR_LANG,
    KNOWN_TESSDATA_LOCATIONS,
    Path,
    TesseractLanguageMissingError,
    annotations,
    ensure_tesseract_ready,
    format_language_install_instructions,
    get_installed_ocr_languages,
    logger,
    logging,
    os,
    pytesseract,
    resolve_effective_ocr_lang,
    setup_tessdata_prefix,
    verify_tesseract_languages,
)

from .models import (
    Any,
    BudgetPaymentDetails,
    Decimal,
    DocumentClassificationResult,
    DocumentType,
    Enum,
    FinancialSummary,
    FiscalMemoryReportDetails,
    GoodsReceiptDetails,
    Invoice,
    InvoiceMetadata,
    LineItem,
    LogicalBlock,
    LogicalLine,
    MIN_CONFIDENCE,
    MoneyAmount,
    OcrToken,
    PageImage,
    PageTransform,
    Party,
    PaymentDetails,
    TableColumn,
    TableRegion,
    ValidationIssue,
    ValidationResult,
    _InvoiceEncoder,
    annotations,
    convert_bgn_to_eur,
    convert_eur_to_bgn,
    dataclass,
    dataclasses,
    field,
    json,
)

from .normalizers import (
    Any,
    Decimal,
    InvalidOperation,
    RECIPIENT_KEYWORDS,
    RECIPIENT_KEYWORD_RE,
    SUPPLIER_KEYWORDS,
    SUPPLIER_KEYWORD_RE,
    annotations,
    clean_ocr_artifacts,
    correct_eik_checksum,
    datetime,
    is_recipient_keyword,
    is_supplier_keyword,
    is_valid_eik13,
    is_valid_eik9,
    logger,
    logging,
    normalize_bic,
    normalize_eik,
    normalize_iban,
    normalize_vat_number,
    parse_date,
    parse_money,
    re,
    repair_eik_mod11,
    sanitize_vat_rate,
    validate_eik,
    validate_iban_modulo97,
)

from .ingestion import (
    DEFAULT_RASTER_DPI,
    Generator,
    IMAGE_EXTENSIONS,
    Image,
    Iterator,
    PDF_EXTENSIONS,
    PageImage,
    Path,
    SUPPORTED_EXTENSIONS,
    _stream_pdf_pages,
    annotations,
    cv2,
    fitz,
    iter_document,
    iter_rasterize_pdf,
    load_document,
    load_image,
    load_image_page,
    logger,
    logging,
    np,
    os,
    pixmap_to_bgr,
    pymupdf,
    rasterize_pdf,
)

from .preprocessing import (
    Any,
    Image,
    PageImage,
    PageTransform,
    adaptive_threshold,
    annotations,
    apply_deskew,
    apply_orientation,
    binarize_dotmatrix,
    binarize_dotmatrix_bridged,
    binarize_otsu,
    check_and_fix_orientation,
    cv2,
    denoise,
    denoise_bilateral,
    deskew_image,
    detect_deskew_angle,
    detect_orientation,
    enhance_contrast,
    enhance_contrast_clahe,
    generate_preprocessing_variants,
    global_threshold,
    logger,
    logging,
    math,
    morphological_cleanup,
    normalize_page_geometry,
    np,
    preprocess_mobile_photo,
    pytesseract,
    sauvola_threshold,
    to_grayscale,
    upscale_if_needed,
    warnings,
)

from .ocr_passes import (
    Any,
    DEFAULT_OCR_LANG,
    MIN_CONFIDENCE,
    OcrToken,
    Output,
    PageTransform,
    _parse_ocr_dict_to_tokens,
    _score_ocr_result,
    annotations,
    build_raw_ocr_evidence,
    clean_ocr_artifacts,
    compute_box_metrics,
    defaultdict,
    execute_ocr_pass,
    fuse_ocr_passes,
    is_line_noise_token,
    logger,
    logging,
    normalize_ocr_tokens,
    np,
    pytesseract,
    run_multiple_ocr_passes,
    score_token_quality,
)

from .layout import (
    ALL_COLUMN_SYNONYMS_SORTED,
    Any,
    CONTINUATION_KEYWORDS,
    CONTINUATION_PATTERNS,
    LogicalBlock,
    LogicalLine,
    OcrToken,
    RECEIPT_KEYWORDS,
    SUMMARY_KEYWORDS,
    TableColumn,
    TableRegion,
    _assign_line_to_columns,
    _assign_token_to_column,
    _create_zoned_block,
    _cross_validate_table_columns,
    _is_summary_line,
    _match_column_synonym,
    _token_vertical_overlap_ratio,
    annotations,
    build_x_projection_profile,
    clean_ocr_artifacts,
    clean_table_crop,
    cv2,
    defaultdict,
    detect_receipt_regions,
    detect_table_regions,
    group_lines_into_blocks,
    group_tokens_into_lines,
    is_recipient_keyword,
    is_supplier_keyword,
    is_transfer_or_header_line,
    logger,
    logging,
    np,
    parse_money,
    re,
    resolve_party_orientation,
)

from .classification import (
    Any,
    BudgetPaymentDetails,
    Decimal,
    DocumentClassificationResult,
    DocumentClassifier,
    DocumentType,
    FiscalMemoryReportDetails,
    GoodsReceiptDetails,
    Invoice,
    InvoiceMetadata,
    LineItem,
    LogicalBlock,
    LogicalLine,
    MoneyAmount,
    OcrToken,
    Party,
    ValidationIssue,
    ValidationResult,
    annotations,
    classify_document,
    clean_ocr_artifacts,
    extract_budget_payment_order,
    extract_credit_debit_note_reference,
    extract_fiscal_memory_report,
    extract_goods_receipt,
    extract_goods_receipt_line_items,
    extract_parties_for_goods_receipt,
    extract_taxpayer_from_fiscal_report,
    logger,
    logging,
    normalize_bic,
    normalize_eik,
    normalize_iban,
    normalize_vat_number,
    parse_date,
    parse_money,
    re,
    validate_budget_payment_order,
    validate_fiscal_memory_report,
    validate_goods_receipt,
)

from .extraction import (
    Any,
    DISQUALIFY_PATTERNS,
    ExtractedDates,
    LEGAL_FORM_PATTERN,
    LogicalBlock,
    LogicalLine,
    OcrToken,
    PARTY_NAME_EXCLUDE,
    Party,
    _dedup_invoice_number,
    _extract_metro_recipient,
    _extract_party_fallback,
    _find_party_region,
    _is_metro_document,
    _sanitize_extracted_date,
    _union_bbox,
    annotations,
    clean_ocr_artifacts,
    clean_party_name,
    dataclass,
    datetime,
    extract_dates,
    extract_due_date,
    extract_invoice_number,
    extract_party,
    extract_place_issued,
    fuse_visual_rows,
    is_valid_eik13,
    is_valid_eik9,
    logger,
    logging,
    normalize_eik,
    normalize_vat_number,
    parse_date,
    re,
    repair_eik_mod11,
    score_party_candidate,
    validate_eik,
)

from .table_recovery import (
    Any,
    Decimal,
    InvalidOperation,
    LineItem,
    LogicalLine,
    MoneyAmount,
    OcrToken,
    TableColumn,
    TableRegion,
    _parse_row_tokens_anchor_guided,
    _sanitize_line_item_candidate,
    annotations,
    build_x_projection_profile,
    clean_ocr_artifacts,
    clean_table_crop,
    cv2,
    execute_ocr_pass,
    extract_fiscal_fuel_receipt_items,
    extract_line_items,
    extract_service_description,
    generate_preprocessing_variants,
    itertools,
    logger,
    logging,
    np,
    parse_money,
    re,
    recover_anchor_guided_table,
    run_multiple_ocr_passes,
    sanitize_vat_rate,
    synthesize_service_line_item,
    to_grayscale,
)

from .financials import (
    Any,
    DUAL_DISPLAY_END_DATE,
    Decimal,
    EUR_MANDATORY_DATE,
    FinancialSummary,
    InvoiceMetadata,
    LogicalBlock,
    LogicalLine,
    MoneyAmount,
    OcrToken,
    PaymentDetails,
    ROUND_HALF_UP,
    _detect_all_currencies,
    _extract_metro_financial_summary,
    annotations,
    calculate_ocr_confidence,
    clean_ocr_artifacts,
    convert_bgn_to_eur,
    convert_eur_to_bgn,
    extract_amount_in_words,
    extract_currency,
    extract_financial_summary,
    extract_payment_details,
    logger,
    logging,
    normalize_bic,
    normalize_iban,
    parse_bg_amount_in_words,
    parse_date,
    parse_money,
    re,
    verify_dual_currency_parity,
)

from .validation import (
    Any,
    DUAL_DISPLAY_END_DATE,
    Decimal,
    DocumentType,
    EUR_MANDATORY_DATE,
    Invoice,
    OcrToken,
    TOTAL_TOLERANCE,
    VAT_TOLERANCE,
    ValidationIssue,
    ValidationResult,
    ZDDS_DISCOUNT_TOLERANCE,
    _validate_amount_in_words,
    _validate_currency,
    _validate_dates,
    _validate_identifiers,
    _validate_line_items,
    _validate_ocr_confidence,
    _validate_required_fields,
    _validate_totals,
    annotations,
    convert_eur_to_bgn,
    is_valid_eik13,
    is_valid_eik9,
    logger,
    logging,
    re,
    validate_budget_payment_order,
    validate_contractor_eligibility,
    validate_eik,
    validate_iban_modulo97,
    validate_invoice,
    verify_dual_currency_parity,
)

from .cache import (
    Any,
    DEFAULT_OCR_CACHE_DIR,
    DEFAULT_OCR_LANG,
    OcrToken,
    Path,
    annotations,
    clear_ocr_cache,
    compute_file_sha256,
    get_ocr_cache_path,
    get_ocr_cache_stats,
    hashlib,
    json,
    load_ocr_cache,
    logger,
    logging,
    save_ocr_cache,
    time,
)

from .pipeline import (
    Any,
    DEFAULT_OCR_CACHE_DIR,
    DEFAULT_OCR_LANG,
    DocumentClassifier,
    DocumentType,
    Invoice,
    MIN_CONFIDENCE,
    OcrToken,
    PageImage,
    PageTransform,
    Path,
    _InvoiceEncoder,
    _extract_and_validate_from_tokens,
    annotations,
    build_raw_ocr_evidence,
    calculate_ocr_confidence,
    cv2,
    detect_receipt_regions,
    detect_table_regions,
    extract_amount_in_words,
    extract_budget_payment_order,
    extract_credit_debit_note_reference,
    extract_currency,
    extract_dates,
    extract_financial_summary,
    extract_fiscal_memory_report,
    extract_goods_receipt,
    extract_invoice_number,
    extract_line_items,
    extract_party,
    extract_payment_details,
    extract_place_issued,
    extract_service_description,
    generate_preprocessing_variants,
    group_lines_into_blocks,
    group_tokens_into_lines,
    iter_document,
    json,
    load_document,
    load_ocr_cache,
    logger,
    logging,
    normalize_ocr_tokens,
    normalize_page_geometry,
    np,
    os,
    process_invoice,
    recover_anchor_guided_table,
    resolve_effective_ocr_lang,
    run_multiple_ocr_passes,
    save_ocr_cache,
    serialize_invoice,
    synthesize_service_line_item,
    time,
    to_grayscale,
    try_digital_pdf_fast_path,
    validate_invoice,
)

from .batch import (
    Any,
    DEFAULT_OCR_CACHE_DIR,
    DEFAULT_OCR_LANG,
    Invoice,
    Path,
    ProcessPoolExecutor,
    _process_batch_file_worker,
    annotations,
    as_completed,
    format_batch_console_report,
    gc,
    json,
    logger,
    logging,
    os,
    process_batch,
    process_invoice,
    serialize_invoice,
    setup_tessdata_prefix,
    sys,
    time,
)

from .cli import (
    DEFAULT_OCR_CACHE_DIR,
    DEFAULT_OCR_LANG,
    Path,
    annotations,
    argparse,
    clear_ocr_cache,
    get_installed_ocr_languages,
    get_ocr_cache_stats,
    json,
    logger,
    logging,
    main,
    os,
    process_batch,
    process_invoice,
    serialize_invoice,
    setup_tessdata_prefix,
    sys,
    verify_tesseract_languages,
)

_SUBMODULES = [
    constants, currency, tesseract_env, models, normalizers, ingestion,
    preprocessing, ocr_passes, layout, classification, extraction,
    table_recovery, financials, validation, cache, pipeline, batch, cli,
    vendor_profiles,
]
for _mod in _SUBMODULES:
    for _name in dir(_mod):
        if not (_name.startswith("__") and _name.endswith("__")):
            if _name not in globals():
                globals()[_name] = getattr(_mod, _name)

__all__ = [name for name in globals() if not (name.startswith("__") and name.endswith("__"))]
